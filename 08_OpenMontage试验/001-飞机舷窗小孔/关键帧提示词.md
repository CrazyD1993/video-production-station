# 关键帧候选状态

S06 purpose: moisture_control_design_variants  
KF3 的所有结构画面必须标注：`不同设计方案示意`。

原 KF1/KF2/KF3 的 A/B/C 文字方案已降级为早期提示词草案，不再作为用户最终审批对象。

Phase 2 的正式规则：

1. 先完成三条参考视频的真实拆解；
2. 每个核心镜头只保留两个明显不同的候选，共六张；
3. 六张图片必须由外部图片工具真实生成并放入 `keyframe-handoff/incoming/`；
4. 通过格式、尺寸、数量和视觉风险检查后，自动生成联系表；
5. 用户只根据真实图片选择 KF1、KF2、KF3；Codex 不代选；
6. 三张图全部批准后，才进入 Seedance 手动交接。

六个独立交接文件位于：`keyframe-handoff/prompts/`。

当前状态：`blocked_by_reference_analysis`。没有参考视频，也没有真实关键帧图片。
