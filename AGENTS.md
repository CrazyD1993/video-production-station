# 视频制作站工作约定

## 工作入口

- 每次继续项目，先阅读 `当前工作台.md`、`00_项目总控/项目说明.md` 和本文件。
- 进入 V2 制作前，必须阅读 `07_V2导演系统/` 的升级方案、职责验收标准及对应选题审核包。
- 内容生产依次沉淀为：事实核查、脚本、分镜、封面标题、发布稿、发布复盘。
- 用户只在选题、脚本/分镜、发布前三个关键节点做确认；其余环节由产品经理 Agent 推进。
- 45-90 秒成片必须先通过导演 Agent 的动态分镜验收，再由冷观众审片 Agent 预审；两项任一不通过，不进入发布确认。
- 画面承担证明，旁白承担解释。静态信息卡只能总结，累计不超过成片时长的 15%。
- 第 003 条前 14 秒 Remotion 原型未获用户确认前，不得制作完整成片；完整时长应在样片确认后重新评估，优先控制在 52-58 秒。
- 逻辑 Agent 角色不等于独立执行程序；实际运行职责单元控制在 6 个以内。

## 内容安全

- 科学、健康、灾害内容先查官方或一手来源，并在事实核查文件中保留链接和结论。
- 不把风险提示写成诊断、预测或绝对承诺；灾害内容以当地气象和应急部门实时通知为准。
- 不直接搬运竞品视频、画面、文案或标题组合。

## GitHub 同步

- 每次有效新增或修改项目文件后，检查变更、提交并推送到项目 GitHub 私有仓库。
- 推送前不纳入凭据、Cookie、原始下载视频、未获授权素材或个人信息。
- GitHub 授权失效时，继续完成本地工作，但在交付中明确提示用户重新授权；授权恢复后优先补推全部待同步提交。

## ChatGPT / Codex GitHub 中继

- 当用户说“读取GitHub中继任务”时，先拉取或读取当前分支最新内容，再读取 `00_项目总控/AI协作中继/CHATGPT_TO_CODEX.md`。
- 只执行 front matter 中 `status: ready` 且尚未完成的任务，不扩大范围。
- 完成后保留任务正文，将任务状态改为 `completed` 并写入完成提交 SHA；覆盖更新 `CODEX_TO_CHATGPT.md` 和 `STATE.yaml`。
- 测试和检查完成后，提交并推送 `experiment/openmontage-pilot`；聊天窗口只简短报告完成状态、回执路径和提交 SHA。
- 中继文档和 GitHub 不得包含密钥、Cookie、账号密码、参考视频、候选图片或生成视频。
