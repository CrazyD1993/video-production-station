---
reviewer: "Reviewer A: Contract QA"
target_commit: fe82d547692b9dd0322e9ae32e9de43e14e16bb1
conclusion: PASS_WITH_CONDITIONS
---

# 素材板 V1 合同审核

## 审核边界与结论

唯一事实证据为 `/private/tmp/petrichor-review-fe82d/`。审核期间未读取中继状态、Production Agent 自评、其他 Reviewer 报告或实时工作区生产资产，也未联网、生成或修改媒体。

核心交付满足合同：13 个锁定镜头连续且时间正确；锁定旁白、时间戳及未变速参数有一致证据；10 个真实母素材代理实际存在、文件互异且复用不超过 2 次；授权必填字段完整；预览技术参数合规；动态缺失清单与镜头计划一致。条件项为素材板 SVG 的“必需文件”定义与快照内容不一致，详见问题 1。

## 快照能证实

### 1. 锁定镜头与时间

`asset-board-v1/shot-plan-v1.yaml` 含且仅含 S01–S13，顺序连续、无空隙或重叠，总跨度 46.80 秒：

`S01 0.00–1.20`、`S02 1.20–3.85`、`S03 3.85–5.69`、`S04 5.69–8.70`、`S05 8.70–12.32`、`S06 12.32–17.10`、`S07 17.10–21.38`、`S08 21.38–24.30`、`S09 24.30–27.35`、`S10 27.35–30.50`、`S11 30.50–35.06`、`S12 35.06–39.77`、`S13 39.77–46.80`。

### 2. 旁白锁定与变速证据

- `tts-natural/petrichor-narration-natural.wav` 可读取；ffprobe 为 PCM s16le、48 kHz、单声道、45.937042 秒，与锁定 45.937 秒一致。
- `tts-natural/petrichor-narration-natural.timestamps.json` 保存完整 219 字请求文本、9 个句段及逐字时间戳；首字 0.375 秒，末字结束 45.587 秒。
- 同一 JSON 的请求字段为 `speech_rate: 0`、`sample_rate: 48000`、`enable_timestamp: true`；`shot-plan-v1.yaml` 同时声明 `narration_locked: true`、`narration_speed_changed: false`。快照内未发现变速产物或变速处理指令。

### 3. 真实母素材、重复与授权

- `assets/real-proxy/` 实际含 10 个外部真实代理：3 个 H.264 视频和 7 张 JPEG；10 个文件 SHA-256 均不同，无字节级重复。
- R01–R03 均为 6.000 秒、720×1280、24 fps、无音频流；7 张照片均可探测，尺寸为 1800 像素宽的 1012–2700 像素高版本。
- `asset-manifest-v1.csv` 与 `shot-plan-v1.yaml` 的双向镜头映射一致；R01、R03、R07 各使用 2 次，其余各 1 次，符合“不超过 2 次”。
- 10 条外部记录的文件名、原始页面、作者、平台、许可类型、许可页、下载日期、使用范围、镜头号及本地路径均非空；本地路径全部存在。

### 4. 文件、预览、越权产物与缺失清单

- 镜头计划、素材表、缺失清单、授权摘要、Seedance 草案、机制 A/B 与土壤进入草图的 SVG/PNG、素材板 JPG/PDF、预览 MP4、QA contact sheet/ffprobe、旁白 WAV/时间戳均存在且可读取。
- 预览实测 18.250 秒、540×960、9:16、24 fps、H.264 High、438 帧，仅 1 条视频流且无音频流；contact sheet 明示“素材板预览·非成片”。快照内没有 46.80 秒完整成片、字幕成片或正式混音。
- `paid_model_calls: 0`；Seedance 文件明确标为 `DRAFT ONLY`，快照内没有 Seedance/付费视频模型生成媒体。
- 正式动态缺口在镜头计划和缺失清单中一致为 S01、S05、S07、S08、S09、S10、S11、S12、S13；S09–S11 在说明中合并记录。现有代理、草图与各镜头状态一致，正式合成门禁保持阻塞。

## 仅凭仓库无法证实

- 无联网条件下，不能独立确认 Pexels 页面当前仍在线、页面作者与许可内容是否发生变化；只能确认快照保存了可复核链接和完整字段。
- 没有外部账单或服务端调用日志，不能证明仓库之外绝对没有付费调用；只能确认快照内无相关产物且声明调用数为 0。
- 没有独立的生成前音频哈希或服务端原始响应签名，不能证明 WAV 从未被重生成或后处理；只能确认其技术参数、时长、文本时间戳与 `speech_rate: 0` 一致，且未发现变速证据。

## 问题

### 问题 1：素材板 SVG 的必需文件契约与快照不一致

- **镜头编号：** 全局
- **时间位置：** 全片
- **证据：** `asset-board-v1/tests/test_asset_board_v1.py` 的 `required` 列表包含 `petrichor-asset-board-v1.svg`；指定快照中该文件不存在，且 `asset-board-v1/.gitignore` 明确排除它。与此同时，实施计划明确交付的是素材板 JPG/PDF，二者均存在且可读取。
- **严重程度：** 中，非阻塞素材板内容审阅，但阻塞“快照内全部 required 文件齐全”的严格断言，因此不能给无条件 PASS。
- **建议：** 下一提交明确唯一文件契约：若 SVG 是正式必需交付，移除忽略规则并纳入快照；若仅为可重建中间产物，则从测试的 required 列表移除，并保留 JPG/PDF 为正式交付。不要在本审核中自动修复。

